package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Quote Gen Dark / Near-Black & Gold Theme (Matching App Icon)
val StudioAppBg = Color(0xFF0D0D0D) // Near-black / dark charcoal canvas
val StudioDarkBg = Color(0xFF0D0D0D)
val StudioSurface = Color(0xFF171717) // Deep charcoal surface for elevated cards, bars, and panels
val StudioSurfaceVariant = Color(0xFF222222) // Elevated dark surface for chips, tracks, and inputs
val StudioCardBorder = Color(0xFF2C2C2C) // Subtle dark gray hairline border
val StudioGoldBorder = Color(0x60F2C200) // Subtle gold hairline border for active elements

// The Perfect Shade of Warm Yellow/Gold Accent (Matching the App Icon)
val StudioPrimary = Color(0xFFF2C200) // Vibrant, luminous warm gold/yellow
val StudioPrimaryVariant = Color(0xFFD4AA00) // Deeper rich gold for active borders and labels
val StudioPrimaryLight = Color(0xFFFFD54F) // Soft bright gold
val StudioSecondary = Color(0xFFE5B800) // Warm amber-gold accent
val StudioTertiary = Color(0xFFFFC107) // Radiant amber gold
val StudioGoldTint = Color(0x22F2C200) // Subtle gold fill for chips and active badges
val StudioGoldSubtle = Color(0x12F2C200) // Whispering pale gold tint
val StudioGoldGlow = Color(0x44F2C200) // Luminous gold halo/glow

// High-Contrast Typography for Dark Background
val StudioTextPrimary = Color(0xFFF5F5F5) // Crisp near-white for primary text
val StudioTextSecondary = Color(0xFFB0B0B0) // Refined medium warm-gray for subtitles & labels
val StudioTextMuted = Color(0xFF757575) // Muted slate-gray for placeholders & subtle hints
val StudioTextOnGold = Color(0xFF0D0D0D) // Near-black text on gold buttons for optimal contrast

// Status Colors
val StudioSuccess = Color(0xFF10B981)
val StudioWarning = Color(0xFFF59E0B)
val StudioError = Color(0xFFEF4444)

// Gradient Accents
val StudioGradientStart = Color(0xFFF2C200) // Warm gold
val StudioGradientEnd = Color(0xFFFFA000) // Deep golden amber


